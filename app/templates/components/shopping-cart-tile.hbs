<div class="container">
  <ul>
  {{#each shoppingCart.cartItems as |item|}}
    <li>
      <h4>{{item.name}} - {{item.cost}}</h4>
      <h4>{{item.category}}</h4>
    </li>
  {{/each}}
  </ul>
</div>
