{{#if item.quantity}}
  <div class="item-tile">

    <h3>{{item.name}} - {{item.cost}}- Quantity Remaining: {{item.quantity}}</h3>
    <button class="btn btn-sm" {{action 'addToCart' item}}>Add To Cart</button>
  </div>
{{/if}}
